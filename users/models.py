from django.db import models
from django.contrib.auth.models import User

# L'application users utilise le modèle User par défaut de Django
# Aucun modèle personnalisé n'est nécessaire pour le moment
